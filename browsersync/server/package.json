{
  "name": "bs-recipes-server",
  "version": "1.0.0",
  "description": "Server example",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "gitUpdate": "git add . && git commit -m'update browsersync,' && git pull && git push"
  },
  "license": "MIT",
  "dependencies": {
    "browser-sync": "^2.1.6"
  }
}
